@page
@model DeliveryCart.Pages.DeliveryPersons.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DeliveryPerson[0].DeliveryPersonFirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DeliveryPerson[0].DeliveryPersonLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DeliveryPerson[0].DeliveryPersonUsername)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DeliveryPerson[0].DeliveryPersonPassword)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DeliveryPerson) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DeliveryPersonFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DeliveryPersonLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DeliveryPersonUsername)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DeliveryPersonPassword)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
